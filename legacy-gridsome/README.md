# Archived Vue/Gridsome site

This directory contains the previous implementation as a read-only design and
content reference. It is intentionally excluded from the active toolchain.

The production application now lives in `src/app`, `components`, and `lib` and
runs on React, Next.js, and TypeScript. Shared content remains in `content`, and
runtime assets are served from `public`.
